# Coffee Scoreboard

## Running

    $ node index.js


## API

### /score

Returns the current score in `Y: { X: count }` format

### /coffee

Parameters:
* x
* y
* direction (up or down)

Returns the current score or error
